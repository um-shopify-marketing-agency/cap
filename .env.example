# cap — Environment variables template
# Copy this file to .env. Nothing required at MVP.

# ─── MVP ───
# No external credentials needed. Trinity Channel Adapter handles Slack
# delivery; cap operates only on its own knowledge/ and memory/ files.

# ─── Post-MVP (uncomment when enabled) ───
# When we wire cap to read Confi-agent's project state for context:
# (placeholder — exact scheme TBD)
